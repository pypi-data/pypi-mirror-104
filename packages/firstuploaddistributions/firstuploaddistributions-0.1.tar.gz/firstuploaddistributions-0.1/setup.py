from setuptools import setup

setup(name='firstuploaddistributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['firstuploaddistributions'],
      zip_safe=False)
