from setuptools import setup

setup(
    name = 'pythw',
    version = '1.0',
    description = ['Pyth Console'],
    package_dir = {'': 'src'},
    install_requires = [
        'pyfiglet'
    ]
)